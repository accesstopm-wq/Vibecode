name: Run GAP

on:
  push:
    paths:
      - 'gap/test.g'
      - '.github/workflows/build-gap.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gap-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gap:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/gap-system/gap:4.15.1-slim
      options: --user 0

    steps:
      - uses: actions/checkout@v4

      - name: Run GAP
        run: gap -q "${GITHUB_WORKSPACE}/gap/test.g" > "${GITHUB_WORKSPACE}/gap/result.txt"

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: gap-result
          path: ${{ github.workspace }}/gap/result.txt

  commit-result:
    needs: gap
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Download result
        uses: actions/download-artifact@v4
        with:
          name: gap-result
          path: gap

      - name: Commit generated result
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add gap/result.txt
          git diff --cached --quiet || git commit -m "Update GAP result"
          git push
